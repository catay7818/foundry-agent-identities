info:
  name: api post
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:7071/api/httppost
  body:
    type: json
    data: |-
      {
          "name": "Awesome Developer",
          "age": 25
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
