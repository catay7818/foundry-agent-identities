info:
  name: api post
  type: http
  seq: 1

http:
  method: POST
  url: "{{functionAppHost}}/api/httppost"
  body:
    type: json
    data: |-
      {
          "name": "Awesome Developer",
          "age": 25
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
